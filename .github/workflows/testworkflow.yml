name: Test workflow
on: push
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Get code
              uses: action/checkout@v4
            - name: intall nodejs
              uses: action/setup-node@v4
              with:
                node-version: 18
            - name: install dependencies
              run: npm ci
            - name: Run test
              run: npm test  


